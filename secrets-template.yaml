# DO NOT COMMIT THIS FILE WITH REAL PASSWORDS
# This is a template - copy to secrets.yaml and fill in real values
# Then add secrets.yaml to .gitignore

apiVersion: v1
kind: Secret
metadata:
  name: xgboost-secrets
  namespace: xgboost-dev
type: Opaque
stringData:
  DB_USER: postgres
  DB_PASSWORD: CHANGE_ME_REPLACE_WITH_REAL_PASSWORD
  # Add other secrets as needed
  # DB_HOST: postgres-dev.xgboost-dev.svc.cluster.local
  # DB_PORT: "5432"
  # DB_NAME: octagona
